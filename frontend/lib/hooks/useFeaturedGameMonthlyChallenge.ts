"use client";

export function useFeaturedGameMonthlyChallenge(challengeType: string) {
  void challengeType;
  // Direct challenge progress writes are disabled. Challenge progress must now
  // be awarded by verified server-side flows, not by feature-page visits.
}
